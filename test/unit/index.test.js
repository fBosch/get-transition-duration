import { assert } from 'chai'
// import { mocks } from 'mock-browser'
// import getTransitionDuration from '../../src/index'
// import jsdom from 'mocha-jsdom'


// describe('getTransitionDuration', () => {
//     jsdom()
//     let div
//     const expectedTransitionDuration = 200,
//           expectedDelay = 50,
//           expectedTransitionDurationWithDelay = expectedTransitionDuration + expectedDelay


//     before(() => {
//         div = document.createElement('div')
//         div.style.transitionDuration = expectedTransitionDuration
//         div.style.transitionDelay = expectedDelay
//     })


//     it('should return the transition duration of the given element', () => {
//         console.log(getTransitionDuration(div))
//         assert(getTransitionDuration(div) === expectedTransitionDuration, 'Function without delay does not return expected value')
//     })
// })
